# Forum app container environment variables
# Copy to .env.forum and fill in values before deploying
# Note: VITE_API_URL is baked into the client bundle at build time (via --build-arg in deploy.sh)
# The env vars here are only used at runtime by the Nitro SSR server

NODE_ENV=production
